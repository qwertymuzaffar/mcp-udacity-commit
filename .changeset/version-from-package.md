---
'mcp-udacity-commit': patch
---

The server now reports the version from package.json instead of a hardcoded string that had stayed at 1.2.2, and `changeset version` keeps server.json in step with it.
